# PDF.js 升級規劃比較

> 狀態：僅比較規劃，尚未更新任何檔案
> 最後整理：2026-09-09

## 版本現況

| 項目 | 版本 | 說明 |
|---|---|---|
| 目前內建 (`lib/pdfjs/`) | **5.3.31** | 2024 年出廠，`pdf.mjs` + `pdf.worker.mjs` 兩個檔 |
| 5.x 系列最新 | **5.7.284** | 同為 5.x，無 breaking change |
| npm 最新穩定 | **6.3.289** | 2026-08-29 發佈 |
| 6.2.108 | 額外重要 | 修掉影響 5.x 的安全公告 |

## 5.3.31 → 6.x 主要功能差異

### 渲染 / 效能

- 6.0 起 Chrome 預設用原生 `ImageDecoder` 解圖（更快、不卡主執行緒）
- WebGPU 渲染（僅 Firefox 支援，可選用、非預設，不會自動開啟）
- SMask / soft-mask、透明度（knockout）、JPX / JBIG2 解碼大幅改善
- Type1 / CFF 字型解析改善
- 文字選取更明顯（含深色模式對比度修正）

### 新功能

- **數位簽章驗證**（`getSignatures` / `getSignatureData`，6.2 新增）
- 聲音註解播放（6.2）
- 加密附件支援（6.0）
- 整詞搜尋改用 `Intl.Segmenter`（6.2）
- 表單欄位重建、comb 欄位渲染
- Optional Content（圖層）在註解層渲染
- Tagged PDF 無障礙：更多結構角色、替代文字、表格屬性
- 合併多份 PDF、插入圖片為新頁等 viewer 功能

### 安全性

- 6.2.108 修掉影響 5.x 的安全公告（GHSA-hq66-cqwq-w95j）
- AES-256 rev5 / rev6 密碼解密修正（含非 ASCII 密碼、SASLprep）

### API 破壞性變更（升級 6.x 要留意）

- `[api-major]` 6.0：
  - 拉高最低瀏覽器支援
    - modern build：需很新的瀏覽器
    - `legacy` build：Chrome 125+ / Safari 18+ / Firefox ESR+
  - `getDocument` 一定要傳物件參數
  - 移除 `PDFDocumentProxy.destroy`（改為 `loadingTask.destroy()`）
  - 移除舊瀏覽器 polyfill
- `[api-minor]` 部分 getter 改回傳 `Map` / `Set`：
  - `getAttachments`、`getDestinations`、`getPermissions`、`getJSActions`、`getFieldObjects`

## 對本專案的影響

### 目前使用到的 API（script.js）

| API | 6.3.289 是否存在 | 結論 |
|---|---|---|
| `getDocument(...)` | ✔ | 不影響（已是傳物件參數） |
| `page.getTextContent()` | ✔ | 不影響 |
| `pdfjsLib.TextLayer` | ✔ | 不影響 |
| `pdfjsLib.Util.transform` | ✔ | 不影響 |

→ 主要程式邏輯**大概率不用改**，可直接換檔。

### 注意事項

1. **瀏覽器支援**：本專案為本機 file:// app，需確認使用者瀏覽器版本。若 < Chrome 125 / Safari 18，6.x 須搭配 `legacy` build。
2. **依賴目錄**：目前 `lib/pdfjs/` 只有 `pdf.mjs` 與 `pdf.worker.mjs`；6.x 還依賴：
   - `cmaps/`
   - `standard_fonts/`
   - `wasm/`
   （正式升級需整個目錄一起換）

## 升級選項

| 選項 | 風險 | 內容 |
|---|---|---|
| 升 **5.7.284** | 低（同 5.x API，無 breaking change） | 多數 bug 修復；**沒有**數位簽章與最新安全修復 |
| 升 **6.3.289** | 中低（換檔後基本可跑，但要盯瀏覽器支援） | 全部新功能 + 安全修復；建議配 `legacy` build 保險 |

## 待辦 / 決策點

- [ ] 確認目標使用者瀏覽器版本分佈（決定 modern / legacy build）
- [ ] 決定採 5.7.284（低風險）或 6.3.289（完整功能）
- [ ] 升級時下載完整 `lib/pdfjs/` 目錄替換
- [ ] 升級後回歸測試：開檔、文字層、選取、搜尋
---

## 評估結論（2026-09-09，對照實際程式碼後）

**沒有功能上的升級必要，唯一實質理由是安全性。**

### 已驗證事項

- `pdf.mjs` 內的 `const version = "5.3.31"` 是唯一可信來源；同名的 `js_version.txt` 內容其實是字串 `js_version.txt`（已刪除），版本改由 UI 直接讀 `pdfjsLib.version` 顯示。目錄下只有 `pdf.mjs` + `pdf.worker.mjs`
- `script.js` 實際用到的 pdf.js API 僅四個：`getDocument`、`page.getTextContent()`、`pdfjsLib.TextLayer`、`pdfjsLib.Util.transform`
- 上表所有 `[api-major]` / `[api-minor]` 破壞性變更**沒有一項打到本專案**（**注意：resetApp 現在會呼叫 `PDFDocumentProxy.destroy()`，升級時要一併驗證**；未用 未用 `getAttachments` / `getDestinations` / `getPermissions` / `getJSActions` / `getFieldObjects`）

### 6.x 新功能對本專案的實際價值

數位簽章驗證、聲音註解、加密附件、合併 PDF、表單 comb 欄位——**本專案沒有對應介面，全部用不到**。
唯一會有感的是 Chrome 原生 `ImageDecoder` 解圖，但只在開大量掃描影像 PDF 時才看得出差別。

### 前述「依賴目錄」一節高估了工作量

`cmaps/` / `standard_fonts/` / `wasm/` 在 5.3.31 同樣是選配資源。本專案只放兩個檔即正常運作，
代表沒走到 CJK 內嵌字型對應與 JPX 解碼那條路徑。**升級時照樣只換兩個檔即可，不需整個目錄替換。**

### 決策依據：會不會開來路不明的 PDF

| 情境 | 建議 |
|---|---|
| 會開外部來源 PDF | 換 **6.3.289 legacy build**（修掉 GHSA-hq66-cqwq-w95j），覆蓋兩個檔，跑下方回歸測試 |
| 只開自己的檔案 | **維持現況**，此項列為待辦即可，風險趨近於零 |

**升 5.7.284 是最沒意義的選項**——承擔了換版風險，卻拿不到那個安全修復（該公告影響整個 5.x）。

### 若決定升級的回歸測試項目

開檔 → 文字層渲染 → 文字選取 → 搜尋
